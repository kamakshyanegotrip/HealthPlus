{
  "note": "Queries that do not resolve against the real schema TODAY, each with the reason and the register item that closes it. See query_contract.mjs. Entries may only be REMOVED — never edited to hide a regression, and never added without a reason. A baselined query that starts passing fails the build, so this list can only shrink. EMPTY as of R10g: 8 at the survey, 3 after the schema half landed, 0 once the cutover deleted the stub — every one of them was the same finding, that the query named the real schema and CI ran the stub. The list being empty is the cutover's own measure of done.",
  "source": "HP-RECON-002 — the R10 cutover survey",
  "entries": []
}
