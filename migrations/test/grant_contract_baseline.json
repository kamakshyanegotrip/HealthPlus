{
  "note": "See grant_contract.mjs. Entries may only be REMOVED, never edited to hide a regression. Migration 031 closed 43 of the 47 findings from this file's first run. Migration 032 closed the one rule-I finding that was a live boundary defect. Migration 034 closed all five rule-C findings (R13-conn, Option 2: one LOGIN role per worker role). Migration 044 closed the obs.response_audit half of SEC-2. **Migration 047 closed the other half and with it the whole of SEC-2**: public.response_audit_event now carries data_region, has RLS with a region-scoped SELECT policy and a region-scoped INSERT policy, and its BEFORE INSERT trigger derives the region from the connection and refuses an append that names a different one. THE STATED REASON THIS ENTRY SURVIVED FOR SIX DAYS WAS WRONG. It read 'adding a column changes what every row_hash covers'; audit_event_chain() hashes an ENUMERATED list of six columns, not the row, so a seventh column is invisible to it — measured, three events verifying 3/3 before and after ADD COLUMN. What 047 does the hard way it does for a different reason: once RLS scopes reads by data_region, an unhashed region column is an undetectable DELETE, so the region goes INSIDE the canonical form and the existing rows are recomputed — refused outright if the log has ever been externally anchored (public.audit_anchor), which is the precondition that expires on the first deployment.",
  "entries": [
    {
      "key": "C-role-reachable|confirmation_ui_role|(role)|NOLOGIN and no members",
      "rule": "C-role-reachable",
      "role": "confirmation_ui_role",
      "object": "(role)",
      "detail": "NOLOGIN and no members",
      "why": "NOT R13-conn — that decision is closed. This role is unreachable ON PURPOSE. Its one grant is INSERT on principal.patient_attribute_confirmation, and nothing in the codebase writes that table: the confirmation UI does not exist yet. Migration 034 §1 withholds LOGIN from roles with no caller, the same reasoning that had migration 031 revoke thirty speculative grants — each individually defensible, none with a caller. dqe_role and erasure_role are in the same position but hold no grants, so rule C does not reach them.",
      "item": "Closes in the migration that builds the confirmation UI (CGP-001 §8.3, the reviewer console). Give it LOGIN there, not before."
    },
    {
      "key": "M-policy-with-no-grant|PUBLIC|principal.patient_attribute|RLS policy p_pa_own exists; no application role holds any privilege",
      "rule": "M-policy-with-no-grant",
      "role": "PUBLIC",
      "object": "principal.patient_attribute",
      "detail": "RLS policy p_pa_own exists; no application role holds any privilege",
      "why": "DELIBERATE, and the only entry of this rule that is. The request path must read patient attributes ONLY through principal.fetch_attribute_envelope, which writes principal.attribute_access_log on every read (§3.8.2) and raises HP-ESC 3.8.2 for inferred rows outside CONFIRMATION_UI. A direct SELECT grant would let the request path read health attributes with no record that it did — which is the guarantee the envelope exists to provide. p_pa_own is therefore the backstop for the day somebody adds that grant anyway, not dead code: migration 041 §3 asserts no such grant exists and fails if one appears.",
      "item": "Permanent. Closes only if attribute access stops being envelope-mediated, which would be a §3.8.2 amendment."
    }
  ]
}
