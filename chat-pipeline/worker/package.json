{
  "name": "healthplus-alert-worker",
  "version": "0.1.0",
  "private": true,
  "description": "RF6 clinician-alert delivery loop (see alert-worker.mjs's header). Deliberately separate from the main app's package.json — this process needs only `pg`, not Next.js/React/the Anthropic SDK/etc. Replaces the side-effect worker, whose queue table existed only in the stub schema.",
  "type": "module",
  "scripts": {
    "start": "node alert-worker.mjs",
    "drain-once": "node alert-worker.mjs --once"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
